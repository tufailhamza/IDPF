/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['react-day-picker', 'embla-carousel-react', 'cmdk', 'vaul', 'input-otp', 'react-resizable-panels', 'next-themes'],
};

export default nextConfig;

